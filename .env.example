# WebSocket Configuration
# Set to enable WebSocket connection (default: disabled in production)
VITE_ENABLE_WS=true

# WebSocket Server URL (optional - defaults to localhost:8080 in development)
# VITE_WS_URL=ws://localhost:8080

# For production, set your WebSocket server URL:
# VITE_WS_URL=wss://your-websocket-server.com

# To disable WebSocket entirely:
# VITE_ENABLE_WS=false